output {
  if "sendmail" in [program] {
    elasticsearch {
      index => "sendmail-"
      hosts => ["elasticsearch:9200"]
    }
  }
  else if "syslog" in [type]{
    elasticsearch {
      index => "syslog-"
      hosts => ["elasticsearch:9200"]
    }
  }
  else{
    stdout{}
    }
}
