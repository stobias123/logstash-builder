output {
  if "sendmail" in [program] {
    elasticsearch {
      index => "sendmail-"
      hosts => ["https://logging-es:9200"]
    }
  }
  else if "syslog" in [type] {
    elasticsearch {
      index => "syslog-"
      hosts => ["https://logging-es:9200"]
    }
  }
  else{
    stdout{}
    }
}
