output {
  if "sendmail" in [program] {
    elasticsearch {
      index => "sendmail-"
      hosts => ["logging-es:9200"]
    }
  }
  else if "syslog" in [type] {
    elasticsearch {
      index => "syslog-"
      hosts => ["logging-es:9200"]
    }
  }
  else{
    stdout{}
    }
}
